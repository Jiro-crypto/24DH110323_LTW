@model WebBE1.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="bg-success">XÓA DANH MỤC</h2>

<h4 class="bg-danger">Bạn thực sự muốn xóa sản phẩm này à?</h4>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            <div class="form-group">
                <div class="control-label col-md-2">Tên Danh Mục</div>
                <div class="cold-md-10">
                    @*Hiển thị giá trị của CategoryName*@
                    @Html.DisplayFor(model => model.CategoryName)
                </div>


                <div class="form-group">
                    <div class="col-md-ofset-2 col-md-10">
                        <input type="submit" value="Xóa" class="btn btn-primary" />
                        @Html.ActionLink("Quay về danh sách", "Index", null, new { @class = "btn btn-primary" })
                    </div>
                </div>
            </div>
        </div>
        
     }
